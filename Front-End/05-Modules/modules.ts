//ts are modular in nature. we can divided our code over several files 
//in ng 8, we then use import {} from to access all the code in those files

//we export a class.interface, variable by adding 'export' in front of it

export class ExportedCalls
{
    //this class is exported
}